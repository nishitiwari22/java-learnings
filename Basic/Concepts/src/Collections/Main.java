public class Main {
    public static void main(String[] args) {
        int arr[] = new int[4];
        //insert an element in an array
        arr[0] = 1;
        //get front element
        int val = arr[0];

        Vector<Integer> vector = new Vector();
        //insert an element in vector
        vector.add(1);
        //get an element
        vector.get(0);
        
    }
}

// Problem is visible, we didn't had a common interface, so it's difficult to remember the methods for each. 
